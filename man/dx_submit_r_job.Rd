% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/dx_submit_r_job.R
\name{dx_submit_r_job}
\alias{dx_submit_r_job}
\title{Submit R Script as DNAnexus Job}
\usage{
dx_submit_r_job(
  script_path,
  instance_type = NULL,
  priority = "normal",
  session_name = NULL,
  tag = NULL,
  include_ofhelper = TRUE,
  remote_inputs = NULL
)
}
\arguments{
\item{script_path}{Character string specifying the path to the R script to be executed}

\item{instance_type}{Character string specifying the DNAnexus instance type.
If NULL (default), uses the lowest instance from the rate card.}

\item{priority}{Character string specifying job priority ("low", "normal", "high").
Default is "normal".}

\item{session_name}{Character string specifying the job name. Default is
'r_job_<timestamp>'.}

\item{tag}{Character string for tagging the job. Default uses timestamp.}

\item{include_ofhelper}{Logical. If TRUE (default), includes the complete
ofhelper package source code to ensure all required functions are available.}

\item{remote_inputs}{Character vector of DNAnexus file paths to be downloaded
into the worker. Default is NULL.}
}
\value{
Job ID of the submitted DNAnexus job
}
\description{
Submits a custom R script as a job to DNAnexus for execution. This function
prepares and executes R scripts in the DNAnexus environment using the Jupyter
workstation app.
}
\examples{
\dontrun{
# Submit an R script with default settings
# job_id <- submit_r_job("path/to/my_script.R")

# Submit with custom parameters
# job_id <- submit_r_job(
#   script_path = "analysis.R",
#   instance_type = "azure:mem2_ssd1_v2_x4",
#   priority = "high",
#   session_name = "my_analysis_job"
# )

# Submit with remote input files
# job_id <- submit_r_job(
#   script_path = "analysis.R",
#   remote_inputs = c("file-xxxxx", "file-yyyyy")
# )
}
}
