% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/tre_rate_card.R
\name{tre_rate_card}
\alias{tre_rate_card}
\title{Get TRE Rate Card}
\usage{
tre_rate_card()
}
\value{
A data.table with the following columns:
  \itemize{
    \item \code{dnanexus_instance_type}: DNAnexus instance type identifiers
    \item \code{azure_instance_type}: Corresponding Azure instance type names
    \item \code{n_gpus}: Number of GPUs (always 0 for TRE instances)
    \item \code{n_cpus}: Number of CPU cores
    \item \code{ram_gb}: RAM in gigabytes
    \item \code{disk_storage_gb}: Disk storage in gigabytes
    \item \code{storage_type}: Storage type description
    \item \code{on_demand_rate_gpb_per_h}: On-demand pricing per hour in GBP
    \item \code{spot_rate_gpb_per_h}: Spot pricing per hour in GBP
  }
}
\description{
Returns a data table containing the rate card information for TRE (The Research
Environment) instance types. This includes pricing, specifications, and availability
information for various Azure virtual machine instances.
}
\examples{
\dontrun{
# Get the TRE rate card
# rate_card <- tre_rate_card()
#
# View the rate card
# head(rate_card)
#
# Find the cheapest instance type
# cheapest <- rate_card[order(on_demand_rate_gpb_per_h)][1]
}
}
