# Get the DNAnexus project ID

Retrieves the currently set DNAnexus project ID from the cache after
validating that a project is set.

## Usage

``` r
dx_get_project()
```

## Value

Character string containing the DNAnexus project ID
