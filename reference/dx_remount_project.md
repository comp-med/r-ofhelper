# Remount DNAnexus Project

Remounts the DNAnexus project file system.

## Usage

``` r
dx_remount_project()
```

## Value

Invisible TRUE if successful

## Examples

``` r
if (FALSE) { # \dontrun{
# Remount project
# dx_remount_project()
} # }
```
