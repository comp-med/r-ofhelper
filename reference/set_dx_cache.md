# Set values in the DNAnexus cache

Assigns one or more values to the DNAnexus cache environment.

## Usage

``` r
set_dx_cache(...)
```

## Arguments

- ...:

  Named arguments to set in the cache

## Value

Invisible NULL
