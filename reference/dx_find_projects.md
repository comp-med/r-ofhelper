# Find DNAnexus Projects

Finds all accessible DNAnexus projects and returns them with names and
IDs.

## Usage

``` r
dx_find_projects()
```

## Value

Named character vector mapping project names to IDs

## Examples

``` r
if (FALSE) { # \dontrun{
# Find projects
# dx_find_projects()
} # }
```
