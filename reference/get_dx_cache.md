# Get values from the DNAnexus cache

Retrieves one or more values from the DNAnexus cache environment.

## Usage

``` r
get_dx_cache(dx_property = "all")
```

## Arguments

- dx_property:

  Character string specifying which property to retrieve. Use "all" to
  retrieve all properties. Default is "all".

## Value

Value(s) from the cache environment
